# Silverstripe CMS inverter

Quasi darkmode

Works with Silverstripe CMS 5 and CMS 6

Simply composer require this module

## Disable the inverter overlay

Set the EMT_INVERTER_DISABLED environment variable to a truthy value (for example `1` or `true`) to disable the inverter behavior without uninstalling the module.
